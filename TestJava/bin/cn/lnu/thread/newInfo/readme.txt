多线程中面试常见的问题：
wait()和sleep()的区别
相同点：都可以让线程处于冻结状态
不同点：
1，wait（）可以指定时间，也可以不指定时间
sleep()，必须指定时间

2，wait()；释放cpu资源，同时释放锁
sleep():释放cpu资源但不释放锁


//如何结束线程？
原理：让run方法结束
	线程任务通常都有循环。因为开启线程就是为了执行需要一些时间的代码。
	只要控制住循环，就可以结束run方法，就可以结束线程
	控制循环，加个标记即可。定义变量